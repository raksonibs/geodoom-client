import { mathOp } from 'red-green-client/helpers/math-op';
import { module, test } from 'qunit';

module('Unit | Helper | math op');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = mathOp([42]);
  assert.ok(result);
});
