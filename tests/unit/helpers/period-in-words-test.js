import { periodInWords } from 'red-green-client/helpers/period-in-words';
import { module, test } from 'qunit';

module('Unit | Helper | period in words');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = periodInWords([42]);
  assert.ok(result);
});
