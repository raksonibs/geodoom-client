import { plusMinus } from 'red-green-client/helpers/plus-minus';
import { module, test } from 'qunit';

module('Unit | Helper | plus minus');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = plusMinus([42]);
  assert.ok(result);
});
