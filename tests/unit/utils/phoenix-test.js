import phoenix from 'red-green-client/utils/phoenix';
import { module, test } from 'qunit';

module('Unit | Utility | phoenix');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = phoenix();
  assert.ok(result);
});
